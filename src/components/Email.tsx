function Email() {
    return ( 
        <div>
            <h1>Email</h1>
            <form>
                <label htmlFor="email">Email</label>
                <input type="text" placeholder="Email" />
            </form>
        </div>
     );
}

export default Email;